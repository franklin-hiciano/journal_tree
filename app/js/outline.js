// outline.js — thread editor removed; stub kept for call-site compatibility
// _onSrcChange calls onTreeParsed() when the tree structure changes.
function onTreeParsed() {
  // nothing to render; run-engine reacts to parsedTree directly
}
